"""BlogForge — a free, style-matched blog generator for pragmaticsysadmin.help.

The package deliberately depends on nothing but the Python standard library so
it costs nothing to run: local inference through Ollama, free hosted tiers
(GitHub Models / Gemini / Groq / OpenRouter) or a no-LLM outline mode.

Entry point::

    python -m blogforge --help
"""

__all__ = ["__version__"]
__version__ = "1.0.0"
